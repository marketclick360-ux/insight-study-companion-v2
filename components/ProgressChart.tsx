import React from 'react';

// Placeholder component to satisfy import in app/(tabs)/progress.tsx.
// Replace with a real chart when Progress is wired to DB data.
export function ProgressChart() {
  return null;
}
