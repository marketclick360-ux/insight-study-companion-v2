// This tab screen re-exports the Topic Registry so it appears as the "Topics" tab.
// The full registry lives in app/topic/index.tsx (also accessible via deep link).
export { default } from '../topic/index';
